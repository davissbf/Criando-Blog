<%- include('../../partials/header.ejs') %>
<body>
  <%- include('../../partials/navbar.ejs') %>
  <div class="container">
    <hr>
    <div class="card"> 
      <div class="card-header">
        <h1>Editar artigo</h1>
      </div>
      <div class="card-body">
        <form method="POST" action="/articles/save">
          <input type="hidden" value="<%= article.id %>" name="id">
          <input class="form-control" type="text" name="title" placeholder="Defina o título do artigo!" value="<%- article.title %>" required><br>
          <textarea class="form-control" placeholder="Escreva seu artigo aqui!" name="body" id="article"></textarea><br>
          <label>Categorias</label>
          <select name="category" class="form-control">
            <% categories.forEach((category) => { %>
              <% if(category.id == article.categoryID) { %>
                <option value="<%= category.id %>" selected><%= category.title %></option>
              <% }else{ %>
                <option value="<%= category.id %>"><%= category.title %></option>
              <% } %>
            <% }) %>
          </select>
          <br>
          <button class="btn style-btn">Atualizar</button>
        </form>
      </div>
    </div>
  </div>
  <div id="content" style="display: none;">
   <%- article.body %>
  </div>
</body>
<%- include('../../partials/footer.ejs') %>

<script src="/tinymce/js/tinymce.min.js"></script>
<!-- <script src="https://cdn.tiny.cloud/1/v7awa6jh4945dhw1v5kid86d1qayn12ee5lhkmjuynhayeke/tinymce/5/tinymce.min.js" referrerpolicy="origin"></script> -->
<script>

  // tinymce.init({
  //   language: 'pt_BR',
  //   selector: '#article',
  //   activeEditor: true,
  //   plugins: [
  //     'advlist autolink link image lists print preview hr searchreplace wordcount fullscreen insertdatatime media save table paste emoticons'
  //   ],
  //   init_instance_callback: () =>{
  //     tinymce.get("article").setContent($("#content").html())
  //   }
  // });

  tinymce.init({
        selector: "#article",
        plugins: [
            'advlist advcode autolink link image lists print preview hr searchreplace wordcount fullscreen insertdatetime media save table paste code autosave emoticons textpattern'
        ],
        toolbar: 'code restoredraft emoticons hr image lists numlist bullist fullscreen',
        themes: "modern",
    })

</script>